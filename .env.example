# Copy this file to `.env` in the project root (see lib/features/AGENTS.md).
# Register the OAuth redirect URL in Supabase Dashboard → Authentication → URL Configuration:
#   io.supabase.flutter://login-callback/

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
